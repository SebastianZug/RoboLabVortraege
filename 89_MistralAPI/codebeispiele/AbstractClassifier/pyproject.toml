[project]
name = "abstract-classifier"
version = "0.1.0"
description = "Abstracts klassifizieren und in strukturierte JSON-Felder zerlegen"
requires-python = ">=3.11"
dependencies = [
    "mistralai>=2.0.0",
    "python-dotenv>=1.0.0",
    "pydantic>=2.0.0",
    "pandas>=2.0.0",
]
