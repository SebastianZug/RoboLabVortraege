[project]
name = "chat-completion"
version = "0.1.0"
description = "Minimalbeispiel: Mistral Chat-Completion via SDK"
requires-python = ">=3.11"
dependencies = [
    "mistralai>=2.0.0",
    "python-dotenv>=1.0.0",
]
